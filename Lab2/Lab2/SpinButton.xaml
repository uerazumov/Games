<UserControl x:Class="Lab2.SpinButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab2"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="24"></Setter>
            <Setter Property="FontStyle" Value="Italic"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Value}" Grid.Column="1" Margin="0,5,0,0" FontSize="20" TextAlignment="Center" VerticalAlignment="Center" FontStyle="Italic"></TextBlock>
        <Button Click="MinusClick" Grid.Column="0" Style="{StaticResource ButtonStyle}">-</Button>
        <Button Click="PlusClick" Grid.Column="2" Style="{StaticResource ButtonStyle}">+</Button>
    </Grid>
</UserControl>
