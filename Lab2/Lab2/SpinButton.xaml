<UserControl x:Class="Lab2.SpinButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab2"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Value}" Grid.Column="1" TextAlignment="Center" FontSize="14" FontStyle="Italic" Margin="0,10,0,0"></TextBlock>
        <Button Click="PlusClick" Grid.Column="0" FontStyle="Italic" FontSize="18">-</Button>
        <Button Click="MinusClick" Grid.Column="2" FontStyle="Italic" FontSize="18">+</Button>
    </Grid>
</UserControl>
